{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Angular: Debug (SSL)",
      "type": "chrome",
      "request": "launch",
      "preLaunchTask": "ng serve ssl",
      "url": "https://localhost:4200/",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
