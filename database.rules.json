{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "homes": {
      "$home": {
        "budgets": {
          "month": {
            ".indexOn": "validityPeriod"
          }
        },
        "entries": {
          ".indexOn": ["monthString","budgetKey","account/key"]
        },
        "regular": {
          ".indexOn": ["cycle"]
        }
      }
    },
    ".read": "auth != null",
    ".write": "auth != null"
  }
}
