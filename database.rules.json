{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "homes": {
      "$home": {
        "budgets": {
          "month": {
            ".indexOn": "validityPeriod"
          }
        },
        "entries": {
          ".indexOn": "monthString"
        }
      }
    },
    ".read": "auth != null",
    ".write": "auth != null"
  }
}
