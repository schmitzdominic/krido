{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "homes": {
      "$home": {
        "budgets": {
          "month": {
            ".indexOn": "validityPeriod"
          }
        }
      }
    },
    ".read": "auth != null",
    ".write": "auth != null"
  }
}
